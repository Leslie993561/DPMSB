@AGENTS.md

# Workflow: git

Depois de qualquer alteração de código pedida pelo usuário (e verificada — build/testes passando), faça commit e `git push origin main` automaticamente, sem perguntar antes. O usuário já autorizou isso de forma permanente nesta pasta.
